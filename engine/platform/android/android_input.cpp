#include "android_input.h"

namespace jaeng::platform {

// The input state is managed inside android_platform.cpp via processInputEvent
// AndroidInput simply serves as the container for the current frame state queried by the application

} // namespace jaeng::platform
